import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'header',
    templateUrl: '../../templates/header/header.html',
    styleUrls: ['../../styles/header/header.css'],
    encapsulation: ViewEncapsulation.None,
})
export class HeaderComponent{
    
}
